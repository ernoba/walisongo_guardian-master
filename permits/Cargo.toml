[package]
name = "permits"
version = "0.1.0"
edition = "2024"

[dependencies]
# winreg digunakan untuk manipulasi Registry secara langsung
winreg = "0.52"

[build-dependencies]
# winres digunakan untuk menempelkan manifest Administrator ke .exe
winres = "0.1"